apiVersion: v1
kind: Service
metadata:
  labels:
    env: demo
  name: clusterip-svc
spec:
  ports:
  - name: clusterip-svc-port
    port: 80
    targetPort: 80
  selector:
      env: demo 
  type: ClusterIP
