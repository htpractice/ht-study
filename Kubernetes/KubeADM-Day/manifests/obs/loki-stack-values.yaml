# obs cluster — Loki + Promtail only (Grafana comes from prometheus chart)
grafana:
  enabled: false

promtail:
  enabled: true
  config:
    clients:
      - url: http://{{ .Release.Name }}:3100/loki/api/v1/push

loki:
  enabled: true
  isDefault: false
  persistence:
    enabled: false
