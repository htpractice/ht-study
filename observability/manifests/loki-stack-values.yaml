grafana:
  enabled: false

promtail:
  enabled: true
  config:
    clients:
      - url: http://{{ .Release.Name }}:3100/loki/api/v1/push
        external_labels:
          cluster: obs

loki:
  enabled: true
  isDefault: false
  persistence:
    enabled: false
